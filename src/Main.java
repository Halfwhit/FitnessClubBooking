/* TODO: Brief:

After each month, the user of the system must print:
 a report containing the number of customers per fitness class, along with the average rating;
 a report containing the fitness class which has generated the highest income.

In your final deliverable, provide data covering at least 5 different types of fitness classes, 15 customers, and 20
reviews (with rating), which can then be displayed in the output reports.
 */
class Main {
    public static void main(String[] args) {
        FitnessClubBooking fitnessClubBooking = new FitnessClubBooking();
        System.out.println(fitnessClubBooking + " has ended.");
    }
}